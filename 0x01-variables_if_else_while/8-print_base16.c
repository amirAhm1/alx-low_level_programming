#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print numbers of base 10'
 * Return: Always 0
  */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (n = 97; n <= 102; m++)
	{
		putchar(m);
	}
        printf("\n");
        return (0);
}
